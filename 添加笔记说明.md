# 添加笔记说明

这个博客是纯静态页面，文章数据现在写在 `assets/app.js` 里的 `posts` 数组中。要添加一篇笔记，复制一段文章对象，放到 `const posts = [` 后面即可。

## 添加步骤

1. 打开 `assets/app.js`
2. 找到 `const posts = [`
3. 复制下面模板，粘贴到数组最前面
4. 修改标题、日期、标签、摘要和正文
5. 保存后刷新 `index.html`
6. 运行 `node tools/generate-post-pages.js`，生成独立文章页

## 文章模板

```js
{
  id: "my-new-note",
  title: "我的新笔记标题",
  date: "2026-06-04",
  readTime: "5 min",
  category: "技术笔记",
  tags: ["Java", "笔记"],
  tone: "blue",
  views: 0,
  likes: 0,
  words: 1200,
  repost: false,
  source: {
    name: "原文作者或网站名",
    url: "https://example.com/original-article"
  },
  summary: "这里写一段文章摘要，会显示在文章卡片里。",
  body: [
    "第一段正文。",
    "第二段正文。",
    "第三段正文。"
  ],
},
```

## 字段说明

- `id`：唯一英文标识，不要和其他文章重复，建议用小写英文和短横线。
- `title`：文章标题。
- `date`：发布日期，格式必须是 `YYYY-MM-DD`。
- `readTime`：预计阅读时间。
- `category`：分类，例如 `技术笔记`、`生活碎片`。
- `tags`：标签数组，会自动进入标签筛选。
- `tone`：卡片色调，可用 `blue`、`rose`、`green`、`amber`。
- `views` / `likes`：静态展示数据，可以先写 `0`。
- `words`：字数，会自动汇总站点总字数。
- `repost`：是否为转载推荐。原创笔记写 `false`，转载推荐写 `true`。
- `source`：原文来源。转载推荐必须填写，原创笔记可以删除这一段。
- `summary`：文章摘要。
- `body`：正文段落数组，每一项是一段文字。

## 转载建议

不要直接复制别人的全文，除非原文明确允许转载并符合它的授权要求。更推荐写成“转载推荐 / 学习摘记”：保留原作者、原文链接和转载标记，正文写自己的摘要、理解和实践清单。

## 哪些数据会自动更新

新增或删除文章后，下面这些数字会自动计算：

- 首页发布数量
- 首页专题数量
- 首页运行天数
- 快捷入口里的专题数和归档数
- 个人资料里的文章数、标签数、总字数
- 右侧站点统计
- 最后活动时间

运行天数的起始日期在 `assets/app.js` 顶部的 `siteConfig.launchDate`，你可以改成自己的建站日期。

## 多页面生成

现在博客支持纯静态多页面。首页的数据仍然维护在 `assets/app.js`，独立文章页由脚本生成：

```powershell
node tools/generate-post-pages.js
```

脚本会生成：

- `posts/index.html`：文章列表页
- `posts/文章id.html`：每篇文章详情页

新增、删除或修改文章后，重新运行一次脚本即可。生成出来的页面都是普通 HTML 文件，不需要后端。
